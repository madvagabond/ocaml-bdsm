(jbuild_version 1)


(executable
 ((name  mux_test )
  (libraries  (
    mux batteries oUnit mirage-flow-unix tcpip.stack-socket
    tcpip.tcpv4-socket tcpip.icmpv4-socket
))))




(alias
 ((name runtest)
  (action (run ${exe:mux_test.exe} -runner sequential ))))
  
