module Headers = struct
  type t = Mux.Headers.t

  let to_cstruct t =
    let card = List.length t in
    write_int16 buf card;

    List.iter (fun kv ->
        write_kv buf kv 
      ) t
  
    
end
